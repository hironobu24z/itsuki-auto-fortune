# タロット機能 統合仕様書

`itsuki-auto-fortune` に、生年月日が不明な相談者向けのタロット鑑定モードを追加する。

## 背景と目的

本サービスは算命学ベースの鑑定アプリだが、生年月日がわからない相談者には対応できない。
タロットは卜術（その瞬間の問いに答える）であり、生年月日を必要としない。
この層を取りこぼさず、最終的には算命学の個別鑑定へ導線をつなぐことが目的。

**別アプリではなく本アプリに統合する。** 理由は、SSE ストリーミング・KeepAlive・鑑定文の文体調整といった既存の実装資産をそのまま使えることと、「生年月日がわからない → じゃあタロットで」という流れが同一アプリ内で完結する方が離脱しないため。

## 最重要の設計原則

**AI にカードの意味を考えさせない。**

シャッフル・抽選・正逆判定はすべてクライアントサイドで完結させ、カードの意味は `tarot-data.json` から引く。Gemini に渡すのは「引かれたカードの意味テキスト・相談内容・名前」のみで、AI の役割はそれらを手紙形式の一続きの文章に統合することだけ。

これは既存の算命学モードの設計（クライアント側で全命式を事前計算し、Gemini には計算をさせない）とまったく同じ思想である。AI に意味を生成させると出力がブレ、データを直しても品質が改善しなくなる。

## 初期版のスコープ

- **大アルカナ22枚のみ**（小アルカナ56枚は将来追加）
- **スプレッド2種**：ワンオラクル（1枚）、スリーカード（3枚：過去/現在/未来）
- **文脈5種**：総合 / 恋愛 / 仕事 / 対人 / 自己
- 正位置・逆位置あり

ケルト十字（10枚）は初期版に含めない。同時解釈する枚数が増えると AI の出力が散漫になるため。

## ファイル構成

### 新規追加

| パス | 内容 |
|---|---|
| `data/tarot-data.json` | 22枚 × 正逆 × 5文脈の意味データ、スプレッド定義。**このファイルが鑑定品質の実体** |
| `public/img/tarot/00-fool.jpg` 〜 `21-world.jpg` | カード画像22枚 |

### 変更

| パス | 変更内容 |
|---|---|
| `api/ai-fortune.js` | `buildTarotPrompt()` を追加。`SYSTEM_INSTRUCTION` にタロット用の分岐を追加 |
| フロントエンド | モード分岐 UI とタロット結果表示セクションを追加 |

## カード画像の入手

Wikimedia Commons の "Rider–Waite tarot deck" カテゴリから取得する。1909年刊行のため著作権保護期間は満了している。
Commons 上のファイル名は `RWS Tarot 00 Fool.jpg` 形式なので、ダウンロード後に `tarot-data.json` の `image` フィールドに合わせて `00-fool.jpg` 〜 `21-world.jpg` へリネームする。

## 入口の導線

既存の生年月日入力画面に、以下を追加する。

```
[生年月日を入力してください]
 ─────────────────
 生年月日がわからない方はこちら → タロットで占う
```

タロットモードに入ったら、入力項目は「名前」「相談内容」「文脈の選択」「スプレッドの選択」の4つのみ。生年月日は一切求めない。

## 鑑定フロー

```
名前・相談内容・文脈・スプレッドを入力
        ↓
クライアント側でシャッフル → 抽選 → 正逆判定
        ↓
tarot-data.json から該当箇所のみ抽出
   （カード名 / 正逆 / 該当文脈の意味 / guidance / ポジションラベル）
        ↓
buildTarotPrompt() でペイロード生成 → Gemini へ
        ↓
SSE ストリーミングで手紙形式の鑑定文を返す
```

## Gemini へ渡すペイロードの形

```json
{
  "name": "相談者の名前",
  "question": "今の仕事を続けるべきか",
  "spread": "スリーカード",
  "context": "仕事・キャリア",
  "cards": [
    {
      "position": "過去／現状",
      "card": "節制",
      "orientation": "正位置",
      "meaning": "他部署や異分野との連携が実る。無理のないペース配分が鍵",
      "guidance": "どちらかに寄せず、両方を少しずつ取る。折衷案が今回は最善策になる"
    },
    {
      "position": "現在／障害",
      "card": "悪魔",
      "orientation": "正位置",
      "meaning": "条件に縛られて動けない。惰性で続けている",
      "guidance": "鎖は自分で外せる位置にある。まず「これは選んで続けている」と認めるところから始まる"
    },
    {
      "position": "未来／結果",
      "card": "死神",
      "orientation": "正位置",
      "meaning": "撤退や方針転換の時期。続けることより手放すことに価値がある",
      "guidance": "終わらせることを失敗と数えない。手放した分だけ、次に入る余白ができる"
    }
  ]
}
```

**この JSON 以外の情報を AI に渡さない。** カードの意味を AI の知識から補わせないこと。

## SYSTEM_INSTRUCTION の方針

既存の算命学用 `SYSTEM_INSTRUCTION` をベースに、算命学固有の用語（主星・従星・天中殺など）だけを除去して流用する。以下の品質基準はそのまま適用する。

1. 審判者ではなく伴走者のポジショニング
2. 困難な情報には必ず建設的な指針をセットで添える（`guidance` フィールドがこれを担保している）
3. 構造化された文書ではなく手紙形式
4. 相手の名前を使った一対一の語りかけ
5. 締めくくりは抽象的なムードではなく具体的な日常の一場面

加えてタロット固有の指示：

- 3枚引きの場合、カードを1枚ずつ順に解説せず、**3枚を一続きの物語として統合する**
- 「〜のカードが出ています」といったカード名の羅列を避け、意味の中身で語る
- 逆位置を「悪い」と表現しない。方向性の違いとして扱う
- 出力はプレーンな散文。マークダウン記法を使わない（既存の指示と同じ）

## 鑑定文の末尾に入れる導線

タロット鑑定の出力の最後に、以下を固定文として付与する（AI に生成させず、アプリ側で追加する）。

> もし生年月日がわかれば、算命学であなたの生まれ持った性質からさらに深く読み解けます。

これがタロットモードを設ける本来の目的（生年月日不明の相談者を拾い、算命学の個別鑑定へつなぐ）を果たす部分。

## 実装上の注意

- **DOM ID をモード間で必ず分離すること。** 過去に相性鑑定で DOM ID の不一致による表示フリーズが発生している。タロット用の要素は `tarot-` プレフィックスで統一する
- 既存の KeepAlive 機構（ゼロ幅スペースによるタイムアウト防止）と `__WAITING__` / `__END_WAITING__` マーカーによる待機メッセージ制御は、タロットモードでもそのまま適用する
- 算命学モードのコードパスに影響を与えないこと。既存の鑑定が壊れていないか、統合後に必ず確認する

## デプロイ

Vercel の GitHub 自動デプロイ。`git push` のみ。CLI からの直接デプロイは行わない。

## 動作確認チェックリスト

- [ ] 生年月日入力画面からタロットモードに入れる
- [ ] 1枚引き・3枚引きの両方でカードが表示される
- [ ] 同じカードが1回の鑑定内で重複して出ない
- [ ] 正位置・逆位置が両方出る
- [ ] 文脈を切り替えると鑑定文の内容が変わる
- [ ] ストリーミングが途中で止まらない
- [ ] 末尾の算命学への導線が表示される
- [ ] 既存の算命学鑑定・相性鑑定が従来どおり動作する
