# 無料版 鑑定文テンプレート仕様

AI を使わずに `tarot-data.json` だけで鑑定文を組み立てる。API 呼び出しゼロ、即時表示。

## 設計方針

無料版と有料版の差は **分量ではなく種類** でつける。

| | 無料版（テンプレート） | 有料版（AI） |
|---|---|---|
| 内容 | カードが示している状況の説明 | 相談内容への返答 |
| 相談内容 | 参照しない | 読んで反映する |
| 名前 | 使う | 使う |
| 3枚の統合 | 型でつなぐ | 文脈として統合する |
| 費用 | ゼロ | API 1回 |

無料版でも読み応えは出す。手を抜いた無料版は個別鑑定への導線として機能しないため。

## 実装場所

`tarot-study.html` と、本体のタロットモード両方。共通関数として実装し、両方から呼ぶ。

カードを引いた直後に **まずテンプレート文章を表示する**。AI ボタンはその下に置く。順序を逆にしない（無料の文章を読んだ人だけが有料に進む導線にするため）。

## つなぎ表現のバリエーション

同じ型を繰り返すと機械的になる。各スロットに 5〜6 パターンを用意し、`crypto.getRandomValues` で選ぶ。ただし **1回の鑑定内で同じ接続表現を二度使わない**（重複表現の禁止は既存の原則）。

```js
const PHRASES = {
  // 導入（名前に続く）
  open: [
    "さん、いま置かれている状況から見ていきます。",
    "さん、カードが並びました。順に読んでいきます。",
    "さん、ここに出ているものをお伝えします。",
    "さん、今回並んだ三枚から見えるものです。",
    "さん、まず全体の流れからお話しします。"
  ],
  // 過去 → 現在
  toPresent: [
    "そこから今に続いているのが、",
    "その流れを受けて、いま向き合っているのは",
    "それが下地になって、現在は",
    "この背景の上に、今あるのが",
    "そこを通ってきた結果として、いまは"
  ],
  // 現在 → 未来
  toFuture: [
    "このまま進んでいくと、",
    "ここから先に見えているのは、",
    "この状態が続いた場合、向かう先は",
    "今の延長線上にあるのは、",
    "そのまま歩いていった場合、"
  ],
  // 指針の導入
  toGuide: [
    "いま意識しておきたいのは、この一点です。",
    "ここで手がかりになるのは、次のことです。",
    "この場面で持っておくといいのは、こういう見方です。",
    "動くとしたら、ここからです。",
    "ひとつだけ、覚えておいてほしいことがあります。"
  ]
};
```

## 組み立ての型

### ワンオラクル（1枚）

```
{name}さん、{open}

{card.meaning}

{toGuide}
{card.guidance}
```

### スリーカード（3枚）

```
{name}さん、{open}

これまでのところにあったのは、{past.meaning}という流れです。

{toPresent}{present.meaning}という状況です。

{toFuture}{future.meaning}という方向です。

{toGuide}
{present.guidance}
```

**指針は「現在」のカードのものだけを使う。** 3枚分すべて並べると散漫になり、どこから動けばいいかが伝わらなくなる。今いる場所に対する指針が一番実行しやすい。

## 文章生成時の処理

`meaning` のテキストは体言止めや「〜こと」で終わっているものが混在している。型に流し込む際、末尾の句点を除去してから接続する。

```js
const trim = s => s.replace(/[。．]\s*$/, '');
```

`meaning` に「。」で区切られた複数文が入っている場合（例：「条件に縛られて動けない。惰性で続けている」）は、そのまま流し込むと文が途中で切れて読みにくい。この場合は最初の文だけを使う。

```js
const firstSentence = s => trim(s).split('。')[0];
```

ただし短くなりすぎるので、**2文ある場合は「〜、そして〜」でつなぐ**方が自然。実装時に両方試して読み比べること。

## 締めの固定文

テンプレート文章の末尾に、以下を必ず付ける。

```
もっと詳しく、{name}さんのご相談そのものに沿って読むこともできます。
```

その直下に AI ボタンを置く。ボタンのラベルは「相談内容に沿って読んでもらう」に変更する（現在は「AIに鑑定文を書いてもらう」）。無料版との差が何なのかがラベルだけで伝わるようにするため。

さらにその下に、既存の算命学への導線を置く。

```
生年月日がわかれば、算命学であなたの生まれ持った性質からも読み解けます。
```

## やってはいけないこと

- カード名を文章中に出さない（「死神が出ています」は不安を煽る。カード名は画像の下に表示済み）
- 「凶」「悪い」「危険」といった断定的な否定語を使わない
- 逆位置を悪い意味として書かない
- 無料版であることを文中で言及しない（「詳しくは有料で」のような書き方をしない）
- 見出しや記号（#、*、-）を使わない

## 検証項目

- [ ] 1枚引き・3枚引きの両方でテンプレート文章が出る
- [ ] 同じカードの組み合わせでも、つなぎ表現が毎回同じにならない
- [ ] 1回の文章内で同じ接続表現が重複しない
- [ ] 文が途中で切れていない、句読点が二重になっていない
- [ ] カード名が文章中に出ていない
- [ ] AI ボタンがテンプレート文章の下にある（上ではない）
- [ ] API を一度も呼ばずに文章が表示される（ネットワークタブで確認）
